/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './src/**/*.{html,js}',  // Scans all HTML and JS files in the `src` folder
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}
